#include <vector>
#include <tuple>

using namespace std;

std::vector<int> wczytajWektor();
std::vector<int> wczytajWektor();
std::tuple<int, int, int, int> znajdz(vector<int> wektor);
float srednia(vector<int> wektor);
std::tuple<int, int> szukajWartosci(vector<int> wektor);
