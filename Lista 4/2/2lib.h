using namespace std;
#include <vector>

std::vector<vector<int>> generujMacierz(int wys, int szer);
std::vector<vector<int>> operacja(const vector<vector<int>> &macierz1, const vector<vector<int>> &macierz2, int wybor);
std::vector<vector<int>> mnozenie(const vector<vector<int>> &macierz1, const vector<vector<int>> &macierz2);

void wyswietlMacierz(const vector<vector<int>> &macierz);
